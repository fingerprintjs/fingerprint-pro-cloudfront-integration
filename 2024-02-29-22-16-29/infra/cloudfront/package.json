{
  "name": "e2e-cloudfront",
  "main": "index.ts",
  "license": "MIT",
  "scripts": {
    "up": "pulumi up --stack e2e --yes",
    "destroy": "pulumi destroy --stack e2e --yes",
    "export": "pulumi stack export --stack e2e --file stack.json"
  }
}
