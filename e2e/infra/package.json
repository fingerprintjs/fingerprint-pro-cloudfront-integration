{
  "name": "e2e-infra",
  "main": "index.ts",
  "license": "MIT",
  "scripts": {
    "lambda:up": "cd lambda && yarn up",
    "lambda:destroy": "cd lambda && yarn destroy",
    "lambda:export": "cd lambda && yarn export",
    "cloudfront:up": "cd cloudfront && yarn up",
    "cloudfront:destroy": "cd cloudfront && yarn destroy",
    "cloudfront:export": "cd cloudfront && yarn export"
  },
  "devDependencies": {
    "@types/node": "^20.11.24"
  },
  "dependencies": {
    "@pulumi/aws": "^5.41.0",
    "@pulumi/awsx": "^1.0.2",
    "@pulumi/pulumi": "^3.0.0"
  }
}
