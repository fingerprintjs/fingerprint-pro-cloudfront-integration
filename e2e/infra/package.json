{
  "name": "e2e-infra",
  "main": "index.ts",
  "license": "MIT",
  "scripts": {
    "lambda:up": "cd lambda && pnpm run up",
    "lambda:destroy": "cd lambda && pnpm run destroy",
    "lambda:export": "cd lambda && pnpm run export",
    "cloudfront:up": "cd cloudfront && pnpm run up",
    "cloudfront:destroy": "cd cloudfront && pnpm run destroy",
    "cloudfront:export": "cd cloudfront && pnpm run export"
  },
  "devDependencies": {
    "@types/node": "^20.17.12"
  },
  "dependencies": {
    "@pulumi/aws": "^6.66.3",
    "@pulumi/awsx": "^2.19.0",
    "@pulumi/pulumi": "^3.145.0"
  }
}
