{
  "name": "e2e-infra",
  "main": "index.ts",
  "license": "MIT",
  "scripts": {
    "lambda:up": "cd lambda && pnpm up",
    "lambda:destroy": "cd lambda && pnpm destroy",
    "lambda:export": "cd lambda && pnpm export",
    "cloudfront:up": "cd cloudfront && pnpm up",
    "cloudfront:destroy": "cd cloudfront && pnpm destroy",
    "cloudfront:export": "cd cloudfront && pnpm export"
  },
  "devDependencies": {
    "@types/node": "^20.11.24"
  },
  "dependencies": {
    "@pulumi/aws": "^6.24.0",
    "@pulumi/awsx": "^2.5.0",
    "@pulumi/pulumi": "^3.107.0"
  }
}
